# [已废弃] 财务字段抽取

本参考文件已废弃。

财务字段抽取（资产负债表、利润表、现金流量表、财务指标计算）现在由后续 PDF 解析 skill（Marker/Surya）和财务字段抽取 skill 负责。

本 skill（公告搜索与PDF来源发现）仅负责：搜索 → 分类 → PDF链接提取 → 轻量验证 → 输出 source_documents[]。

详细输出格式见 `output-schema.md`。
